#include "sudoku_init.h"

/*
void Initialize_sudoku() {
    // Posiziona il tile "empty" in alto a sinistra del BG0
    u16* map = BG_MAP_RAM_SUB(25); // Usa BG_MAP_BASE(25) configurato per BG0
    map[0] = 0; // Primo tile della mappa (alto a sinistra)
    for (int i=0; i<2; i++) {
        for (int j=0; j<2; j++) {
            numbers_matrix[i*32+j] = (u16)(i*2+j)+16*1
        }
    }
}
*/
